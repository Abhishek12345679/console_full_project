import 'package:console_full_project/console_full_project.dart'
    as console_full_project;
import 'package:console_full_project/console_full_project.dart';

void main(List<String> arguments) {
  try {
    // print(
    //     'Hello world: ${console_full_project.calculate()}! \n Args sum: ${arguments.fold(0, (previousValue, element) => previousValue + int.parse(element))}');

    // cascadeOp();
    // nullAwareSpreadOP();
    // mapOp();

    // var a = makeCar('lkl');
    // print(a('SEWA'));
  } catch (err) {
    print(err);
  }
}
